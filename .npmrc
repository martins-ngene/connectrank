# Prevent npm and pnpm from executing dependency lifecycle scripts (preinstall/postinstall)
ignore-scripts=true

# Ensure pnpm resolves hoisted dependencies cleanly if invoked
shamefully-hoist=false
auto-install-peers=true